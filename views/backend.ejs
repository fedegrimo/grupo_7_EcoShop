
<!DOCTYPE html>
<html lang="en">
<%- include('./layout/head') %>
<body>
   <div class="formlogin-backend">
        <section class="login-form contenedor">

            <form id="frm" method="post" action="/backend">

                <div href="#" class="login-logo">
                    <img src="/img/logo-foot.png">
                </div>

            <h1 class="login-form-title">LOGIN ADMIN</h1>

            <% if (loginFail) { %>

                <div class="text-danger">
                    <span class="msj-error">Error en inicio de sessión. Verificá tu usuario y contraseña.</span>
                </div>

            <% } %>

            <div class="">
                <input type="text" name="email" placeholder="E-mail"value="<%= locals.oldData ? oldData.email : null %>" />
            </div>

            <% if (locals.errors && errors.email) { %>
                <div class="text-danger">
                    <span class="msj-error"><%= errors.email.msg %></span>
                </div>
            <% } %>

            <div class="row">
                <input type="password" name="password" placeholder="Contraseña" value="<%= locals.oldData ? oldData.password : null %>"/>
            </div>

            <% if (locals.errors && errors.password) { %>
                <div class="text-danger">
                    <span class="msj-error"><%= errors.password.msg %></span>
                </div>
            <% } %>

            <div class="">
                <input type="submit" value="Ingresar" class="btn-login"/>
            </div>
                </form>
        </section>
    </div>

   <%- include('./layout/head') %>
   </body>

   </html>
</body>
</html>